{
  "adapterId": "worldpac-speeddial",
  "name": "Worldpac speedDIAL 2.0",
  "url": "https://speeddial.worldpac.com",
  "mode": "browser",
  "needsAuth": true,
  "maxRPS": 1,
  "timeout": 60000,
  "retries": 1,
  "tags": ["us", "commercial", "auth", "browser"],
  "version": 1,
  "lastModified": "2026-04-19T00:00:00Z",
  "lastModifiedBy": "migration",
  "prompt": {
    "customInstructions": "speedDIAL 2.0 is a React SPA. Login at '/#/login' with Material-UI inputs #username and #password, then submit via button[type=submit]. After login the URL settles on '/#/' (home). Search: fill '#searchTerm' (on home or any page) and press Enter; the app routes to '/#/pna' (Price & Availability) and renders '.product-quote' cards. Authentication posts to 'https://worldpac.my.site.com/sdpro/servlet/servlet.loginwidgetcontroller?type=login' behind the scenes; the adapter lets the SPA drive that.",
    "selectorHints": {
      "loginUser": "#username",
      "loginPass": "#password",
      "loginSubmit": "form[data-testid=\"login-form\"] button[type=\"submit\"]",
      "searchInput": "#searchTerm",
      "resultRow": ".product-quote",
      "partImg": ".product-quote img.sd-part-image",
      "brandImg": ".product-quote img.sd-brand-image",
      "descriptionTitle": ".product-quote .product-description .bold-text",
      "productId": ".product-quote .product-detail-link.product-id",
      "priceRow": ".product-quote .name-value-row",
      "availabilityLabel": ".product-quote .item-availability"
    },
    "knownIssues": "Pre-filled username on first load (browser autofill). Always overwrite via native setter + input/change events so React state updates. Login failures surface as a div containing 'Failed to sign in' — no role=alert. Session cookie is set on worldpac.com domain via Cloudflare. Product-detail route ('#/product-detail') is driven by React state — clicking '.product-detail-link' navigates but does NOT put the product id in the URL, so there is no shareable per-product link; sourceUrl on every PartResult stays '/#/pna' and the internal productCode is exposed via raw.productCode / raw.productDetailsApi.",
    "apiNotes": "Internal REST (authenticated via the session cookie, not public): POST '/sdpro/servlet/servlet.loginwidgetcontroller?type=login' (login, host worldpac.my.site.com), POST '/v3/pna360' (search), GET '/v3/productdetails?productCode=<partNumber padded to 18 with spaces><brandCode>' (e.g. 'H11.BX            OSR'), GET '/v3/buyersguide/?productCode=...&searchOptions=AM,OE', POST '/v3/digitalassets' (images). Brand code is parsed from the logo URL path: 'img.wp-static.com/brands/<CODE>.gif'. WCAP API is the public path but needs an API token issued from Settings → API Tokens (not used here)."
  }
}
