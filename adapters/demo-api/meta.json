{
  "adapterId": "demo-api",
  "name": "Demo API (httpbin.org)",
  "url": "https://httpbin.org",
  "mode": "api",
  "needsAuth": false,
  "maxRPS": 5,
  "timeout": 8000,
  "retries": 1,
  "tags": ["demo", "test", "api"],
  "version": 1,
  "lastModified": "2026-04-19T00:00:00Z",
  "lastModifiedBy": "migration",
  "prompt": {
    "customInstructions": "httpbin.org is an HTTP testbed — we use GET /json which returns a fixed JSON document, then synthesize PartResult[] entries from its fields. Exists purely to demonstrate the api mode runtime wiring.",
    "selectorHints": {},
    "knownIssues": "httpbin returns the same payload for any /json request — result count will always be the same.",
    "apiNotes": "Endpoint used: GET /json → { slideshow: { title, slides: [...] } }. We map slides[] into PartResult[]."
  }
}
