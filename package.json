{
  "name": "@interparts/adapters",
  "version": "0.1.0",
  "private": true,
  "description": "Supplier adapters for Inter Parts Aggregator. Each adapter in adapters/<siteId>/ implements PartSearchAdapter.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
